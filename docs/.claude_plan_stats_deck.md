# Plan d'action : stats d'un deck

> Feuille de route privée de la branche `feat/stats-deck` : l'objectif, le chemin, et où on en est.
> Légende : ✅ fait · 🔄 en cours · ⬜ à faire · ⏸️ reporté (autre branche)

**Dernière mise à jour :** 2026-09-24

## 🎯 Objectif

Une commande qui transforme `games.csv` en un rapport lisible par deck :

```
  📊 games.csv            ⚙️ logiciel                              📈 rapports
┌──────────────────┐    ┌────────────────────────────────────┐    ┌─────────────────────────┐
│ 1 ligne par game │    │ python -m dcprepa stats <tournoi>  │    │ stats/terra-midrange.md │
│ deck, oppo,      │ ─> │                                    │ ─> │ winrate, OTP/OTD,       │
│ position, W/L…   │    │ lire, calculer, écrire             │    │ sources, matchups…      │
└──────────────────┘    └────────────────────────────────────┘    └─────────────────────────┘
         ▲
  🃏 decks/<deck>.yaml  (nom, commandant, statut, versions)
```

**Terminé quand :** `python -m dcprepa stats relicfest-2026` écrit un `stats/<deck>.md` par deck,
conforme au modèle `_modele-deck.md`, avec des tests pour chaque calcul.

**Hors de cette branche** ⏸️ : `synthese.md`, poids méta, winrate attendu, matchups non testés
(ils ont besoin du méta MTGTop8 → branches `feat/import-meta` puis `feat/stats-synthese`).

## 🗺️ Le chemin

```
 ✅ 0. Spec prête            rapports-modèles + conventions (stats/README.md)
 ✅ 0. Branche créée         feat/stats-deck
        │
 ✅ 1. Décisions             BO3 = 2+ games, tri par games, rapport vide    🤔
        │
 ✅ 2. Lire les données      read_games() + fiches deck complètes           📂
        │
 ✅ 3. Brique winrate        un Winrate + son affichage « 12.2 % (11/90) »  🧱
        │
 ✅ 4. Calculs du deck       games ET BO3 : général, versions, source,      🧮
        │                    matchups, self-play ; position par game
        │
 ✅ 5. Rapport Markdown      produire le texte de stats/<deck>.md           📝
        │
 ✅ 5b. Méta s'il existe     tri des matchups par poids, colonne remplie    🌍
        │
 ✅ 6. Service               generate_stats() : enchaîne et écrit           ⚙️
        │
 ✅ 7. Commande              MODULES["stats"] dans __main__.py              ▶️
        │
 🔄 8. Essai + doc           test_tournoi, puis RelicFest ; PR vers main    🚀
```

## 🧱 Les fichiers, couche par couche

Même architecture que l'import : chaque couche n'utilise que celles du dessous.

```
src/dcprepa/
├── __main__.py                ✅ ajouter le module « stats »                ▶️
├── services/
│   └── stats.py               ✅ generate_stats() → StatsReport             ⚙️
├── domain/                    (calculs purs, sans fichier)
│   ├── winrate.py             ✅ Winrate, format « 12.2 % (11/90) », ⚠️     🧱
│   ├── stats.py               ✅ tous les calculs d'un deck                 🧮
│   └── report.py              ✅ DeckStats → texte Markdown                 📝
└── storage/                   (seul accès à data/)
    ├── games.py               ✅ + read_games()                             📂
    ├── decks.py               ✅ + fiche complète (name, commandant, statut) 🃏
    ├── meta.py                ✅ load_latest_meta() (fichier le plus récent) 🌍
    └── stats.py               ✅ write_report() (écriture atomique .tmp)    💾

src/tests/                     ⬜ un fichier de tests par fichier de code    🧪
```

## 🔎 Les étapes en détail

### ✅ 1. Décisions 🤔

- ✅ **BO3 ou BO1** : un BO de **2 ou 3 games** est un BO3 ; un BO d'**une seule game** est un BO1.
  Les BO1 comptent dans les winrates par game, pas dans « Par BO3 ».
- ✅ **Tri des matchups sans méta** : par nombre de games (décroissant) ; colonne « Poids méta » à `—`.
- ✅ **Fiche deck complète** : nouvelle fonction `load_deck_sheets()` ; `load_decks()` (import) n'est pas touchée.
- ✅ **Deck sans aucune game** : rapport généré quand même, rempli de `—` (un rapport par fiche).
- ✅ **Qui code** : Claude code, l'utilisateur relit ; une étape à la fois, validée avant la suivante.

### ✅ 2. Lire les données 📂

```
games.csv ──read_games()──> [ {date, match_id, game, source, deck, version, oppo, position, resultat, note/ressenti}, … ]
decks/*.yaml ──load_deck_sheets()──> { "terra-midrange": {name, commandant, statut, versions} }
```

- ✅ `read_games(path)` → `(games, errors)` : en-tête, 10 colonnes, OTP/OTD, W/L ; erreurs avec n° de ligne ; games = dicts.
- ✅ `load_deck_sheets(tournament_dir)` → `(sheets, errors)` : versions via `load_decks()`, + name / commandant / statut (`""` si vide).
- ✅ 22 tests (`test_games.py`, `test_decks.py`) ; suite : 283 OK ; essayé sur test_tournoi (19 games) et RelicFest (0).

### ✅ 3. Brique winrate 🧱

Tout le rapport repose sur une seule brique :

```
Winrate(wins=66, total=120)
   │
   ├── 10 ou plus     →  "55 % (66/120)", "12.2 % (11/90)"
   ├── moins de 10    →  "⚠️ 33.3 % (1/3)"
   └── aucune game       →  "—"
```

- ✅ Pas d'arrondi à l'entier : au dixième, décimale nulle omise (`55 %`, `12.2 %`) ; `format_percent()` sert aussi à l'écart (`+3.2`).
- ✅ ⚠️ sous 10 (games, ou BO3) ; `—` si aucune game ; `rate` exact pour les calculs.
- ✅ 27 tests (`test_winrate.py`) ; suite : 310 OK.
- ✅ Décision : winrate par game (toutes les games) et winrate BO3 (BO3 de 2-3 games) toujours distincts ;
  rapports-modèles et conventions `stats/README.md` mis à jour (Versions et Source avec les deux).

### ✅ 4. Calculs du deck 🧮

```
games du deck ──┬── self-play (oppo « deck@version »)  ──> section Self-play   🪞
                │
                └── le reste ──┬── général (games + BO3, 1-1 = non gagné)       📊
                               ├── par version : nb + écart games / BO3         🔢
                               ├── OTP / OTD (par game seulement)               🎲
                               ├── paper / cockatrice / mtgo (games + BO3)      🌐
                               └── par oppo (games + BO3, OTP/OTD dès 10)       ⚔️
```

- ✅ `compute_deck_stats(games, deck, versions)` → `DeckStats` ; `Record(games, bo3)` porte les deux winrates.
- ✅ Général : toutes versions réunies.
- ✅ BO3 : regrouper par `match_id`, 2 games ou plus, gagné = 2 victoires.
- ✅ Écart d'une version, games et BO3 à part : son winrate − moyenne simple des winrates des autres versions (`+3.2`, `—` si seule).
- ✅ Matchups : triés par games puis nom ; OTP / OTD seulement à partir de 10 games contre l'oppo.
- ✅ Matchups : meilleure version contre l'oppo (par game et par BO3), écart au winrate du matchup (`best_version`).
- ✅ Self-play : mêmes colonnes, exclu de tout le reste.
- ✅ 15 tests (`test_stats.py`) ; suite : 325 OK ; essai sur test_tournoi vérifié à la main.

### ✅ 5. Rapport Markdown 📝

- ✅ `render_deck_report(deck, sheet, stats, generated)` → le texte de `stats/<deck>.md`.
- ✅ `_modele-deck.md` n'est pas modifié : il sert de spec, le texte produit suit sa structure (titres, tableaux, ordre).
- ✅ « Winrate attendu » et « Poids méta » à `—` (méta pas encore là) ; matchups « Triés par nombre de games (pas encore de méta) ».
- ✅ 11 tests (`test_report.py`), dont deck vide = le modèle avec son en-tête rempli ; suite : 336 OK.

### ✅ 5b. Méta s'il existe 🌍

- ✅ `storage/meta.py::load_latest_meta()` : `meta/AAAA-MM-JJ.csv` le plus récent ; pas de méta = pas d'erreur ; méta invalide = erreur bloquante.
- ✅ Avec méta : matchups triés par poids (oppos hors méta à la fin), « Poids méta » rempli, en-tête `meta/<fichier>`, « Triés par poids dans le méta ».
- ✅ Sans méta : tri par games, « Triés par nombre de games (pas encore de méta) ».
- ✅ 21 tests ; suite : 357 OK.

### ✅ 6. Service ⚙️

```
generate_stats(tournament_dir)
   │
   ├── 1. lire games.csv + fiches deck + méta (une erreur → rien n'est écrit 🛡️)
   ├── 2. pour chaque deck : calculer, rendre le Markdown
   ├── 3. écrire stats/<deck>.md              (fichier .tmp puis remplacement 💾)
   │
   └── StatsReport(decks, games, errors, warnings)
```

- ✅ Même règle que l'import : tout est préparé avant la moindre écriture.
- ✅ Avertissements : deck de `games.csv` sans fiche (pas de rapport), version jouée absente de la fiche.
- ✅ 12 tests (écriture + service sur mini-dépôt et test_tournoi copié) ; suite : 369 OK.

### ✅ 7. Commande ▶️

- ✅ `MODULES["stats"]` dans `__main__.py` : `python -m dcprepa stats <tournoi>`.
- ✅ Bilan affiché : `✅ 1 rapport(s) écrit(s) à partir de 19 game(s) : test-deck.` + ligne méta ; ❌ / ⚠️ comme l'import.
- ✅ 4 tests (`tests/test_main.py`) ; suite : 373 OK.

### 🔄 8. Essai et livraison 🚀

- ✅ Essai sur `test_tournoi` (jeu d'essai fictif : 3 decks, 404 games, méta), chiffres vérifiés par l'utilisateur et par un calcul indépendant ; puis RelicFest (rapport vide).
- ✅ Suite pytest complète verte : 373 OK.
- ✅ `src/README.md` : commande `stats` ajoutée.
- ✅ `stats/README.md` (template, RelicFest, test_tournoi) et README du template : rapports générés par `python -m dcprepa stats <slug>`.
- ✅ Doc : page `docs/03-architecture/stats.md` publiée (depuis le brouillon validé).
- ⬜ PR `feat/stats-deck` → `main`.

## 🔭 Après cette branche

Voir `docs/.claude_feuille_de_route.md` pour l'ensemble des branches.

```
✅ import de l'inbox ─> 🔄 stats d'un deck ─> ⏸️ import méta MTGTop8 ─> ⏸️ synthese.md
                         (cette branche)       feat/import-meta          feat/stats-synthese
```
