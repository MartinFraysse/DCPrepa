# Plan d'action : <sujet de la branche>

> Modèle de plan d'une branche : à copier en `docs/.claude_plan_<sujet>.md` au début de la branche, puis remplir.
> Feuille de route privée de la branche `<type>/<description>` : l'objectif, le chemin, et où on en est.
> Légende : ✅ fait · 🔄 en cours · ⬜ à faire · ⏸️ reporté (autre branche)

**Dernière mise à jour :** AAAA-MM-JJ

## 🎯 Objectif

<Une phrase : ce que la branche apporte, vu de l'utilisateur.>

```
  <entrée>                ⚙️ logiciel                              <sortie>
┌──────────────────┐    ┌────────────────────────────────────┐    ┌─────────────────────────┐
│ <fichier lu>     │ ─> │ python -m dcprepa <module> <slug>  │ ─> │ <fichier produit>       │
└──────────────────┘    └────────────────────────────────────┘    └─────────────────────────┘
```

**Terminé quand :** <critère vérifiable : commande, fichier produit, tests>.

**Hors de cette branche** ⏸️ : <ce qui est volontairement laissé à une autre branche, et laquelle>.

## 🗺️ Le chemin

```
 ⬜ 1. Décisions             <questions à trancher avant de coder>           🤔
        │
 ⬜ 2. <étape>               <résumé en quelques mots>                       📂
        │
 ⬜ 3. <étape>               <résumé>                                        🧱
        │
 ⬜ 4. Service               <fonction>() : enchaîne et écrit                ⚙️
        │
 ⬜ 5. Commande              MODULES["<module>"] dans __main__.py            ▶️
        │
 ⬜ 6. Essai + doc           test_tournoi, puis tournoi réel ; PR vers main  🚀
```

## 🧱 Les fichiers, couche par couche

Même architecture que le reste : chaque couche n'utilise que celles du dessous.

```
src/dcprepa/
├── __main__.py                ⬜ ajouter le module « <module> »             ▶️
├── services/
│   └── <sujet>.py             ⬜ <fonction>() → <Bilan>                     ⚙️
├── domain/                    (calculs purs, sans fichier)
│   └── <sujet>.py             ⬜ <rôle>                                     🧮
└── storage/                   (seul accès à data/)
    └── <sujet>.py             ⬜ <lecture / écriture>                       📂

src/tests/                     ⬜ un fichier de tests par fichier de code    🧪
```

## 🔎 Les étapes en détail

### ⬜ 1. Décisions 🤔

- ⬜ **<question>** : <réponse retenue, une fois tranchée>.
- ⬜ **Qui code** : <Claude code, l'utilisateur relit / l'utilisateur code, Claude guide> ; une étape à la fois, validée avant la suivante.

### ⬜ 2. <étape> 📂

- ⬜ <tâche> ; <règle ou cas limite>.
- ⬜ Tests : <cas à couvrir>.

### ⬜ 4. Service ⚙️

- ⬜ Même règle que l'import et les stats : tout est préparé avant la moindre écriture (tout ou rien).
- ⬜ Bilan renvoyé : <compteurs, erreurs, avertissements>.
- ⬜ Tests d'intégration sur un mini-tournoi dans `tmp_path`.

### ⬜ 5. Commande ▶️

- ⬜ `MODULES["<module>"]` dans `__main__.py` : `python -m dcprepa <module> <slug>`.
- ⬜ Bilan affiché : `✅ …`, `❌ …`, `⚠️ …`.

### ⬜ 6. Essai et livraison 🚀

- ⬜ Essai sur `test_tournoi`, puis sur le tournoi réel.
- ⬜ Suite pytest complète verte.
- ⬜ `src/README.md` : commande ajoutée ; README de `data/` concernés à jour.
- ⬜ Doc (sur demande) : page de `docs/`.
- ⬜ PR `<type>/<description>` → `main`.

## 🔭 Après cette branche

Voir `docs/.claude_feuille_de_route.md` pour l'ensemble des branches.

```
✅ <branche précédente> ─> 🔄 <cette branche> ─> ⏸️ <branche suivante>
```
