# Plan d'action : services de saisie

> Feuille de route privée de la branche `feat/saisie` : l'objectif, le chemin, et où on en est.
> Légende : ✅ fait · 🔄 en cours · ⬜ à faire · ⏸️ reporté (autre branche)

**Dernière mise à jour :** 2026-09-24

## 🎯 Objectif

Le logiciel sait remplir lui-même tous les fichiers de saisie (tournoi, games, decks, oppos) : la future GUI n'aura plus qu'à
afficher un formulaire et appeler ces services, sans jamais éditer un fichier à la main.

```
  🖥️ GUI (plus tard)          ⚙️ services de saisie (cette branche)              📂 data/
┌──────────────────┐    ┌──────────────────────────────────────────┐    ┌──────────────────────────────┐
│ formulaire       │    │ tournoi   create / edit                  │    │ tournament.yaml              │
│ (champs remplis) │ ─> │ games     add / edit / delete            │ ─> │ games.csv                    │
│                  │    │ decks     create / version / statut …    │    │ decks/<deck>.yaml, _alias    │
│                  │    │ oppos     add_oppo                       │    │ oppos.yaml                   │
└──────────────────┘    └──────────────────────────────────────────┘    └──────────────────────────────┘
```

**Terminé quand :** chaque service est codé et testé (pytest), vérifie ses entrées avant d'écrire (tout ou rien),
et les fichiers qu'il produit sont relus sans erreur par `import`, `meta` et `stats`.

**Hors de cette branche** ⏸️ : l'interface graphique (`feat/gui`), y compris les services de **lecture** dont ses écrans auront besoin
(liste des tournois, des decks et versions, des oppos pour l'autocomplétion ; `load_deck_sheets` et `load_oppos` existent déjà) ;
pas de CLI complète (décision 2026-09-24).

## 🧰 Les services

Légende : 🟢 indispensable · 🟡 utile · ⚪ confort · ✅ existe déjà. Tous validés par l'utilisateur (2026-09-24) ; l'ordre suit la priorité.

### 🏆 Tournoi (`tournament.yaml`)

| Service | Rôle | |
|---|---|---|
| `create_tournament` | nouveau dossier copié depuis le modèle | 🟢 |
| `edit_tournament` | modifier la date, le lieu, la banlist, les notes | 🟡 |

### 🎮 Games (`games.csv`)

| Service | Rôle | |
|---|---|---|
| `add_games` | saisir une session (BO3 / BO1), avec les contrôles de l'import | 🟢 |
| `edit_game` | corriger une game : position, résultat, note (ex. L → W) | 🟢 |
| `edit_match` | corriger un BO entier : date, source, deck, version, oppo (toutes ses games) | 🟢 |
| `delete_game` | supprimer une game d'un BO | 🟡 |
| `delete_match` | supprimer un BO entier (par `match_id`) | 🟡 |
| `import_inbox` | les games saisies sur téléphone | ✅ |

Sans ces services, une erreur de saisie oblige à rouvrir `games.csv` à la main. Une game se désigne par son `match_id` et son numéro (`game`).

### 🃏 Decks (`decks/<deck>.yaml`)

| Service | Rôle | |
|---|---|---|
| `create_deck` | nouvelle fiche, avec la version v1 | 🟢 |
| `add_version` | nouvelle version (in / out / liste / notes) | 🟢 |
| `set_status` | `envisage` → `retenu` / `ecarte` | 🟢 |
| `edit_deck` | corriger le nom ou le commandant | ⚪ |
| `add_deck_alias` | appellations du deck (`_alias.yaml`) | ⚪ |

### ⚔️ Oppos (`data/oppos.yaml`)

| Service | Rôle | |
|---|---|---|
| `add_oppo` | nouvel oppo, ou nouvelle variante de nom | 🟡 |
| `import_meta` | ajoute les oppos du top 20 | ✅ |

Un oppo inconnu n'est pas bloquant (avertissement, la game est écrite avec le nom saisi, comme à l'import) : `add_oppo` sert à éviter
les noms en double dans les stats (*Ragavn* / *Ragavan*) ; la GUI pourra proposer « Ajouter *Ragavn* comme nouvel oppo, ou comme variante de *Ragavan* ? ».

### 📈 Déjà faits

`import_inbox`, `import_meta`, `generate_stats` (rapports et synthèse) ✅.

## 🗺️ Le chemin

```
 ✅ 1. Décisions             écriture des games et des fiches, champs        🤔
        │
 ✅ 2. Games                 🟢 add_games, edit_game, edit_match             🎮
        │                    🟡 delete_game, delete_match
        │
 ✅ 3. Oppos                 🟡 add_oppo (évite les noms en double)          ⚔️
        │
 ✅ 4. Tournoi               🟢 create_tournament · 🟡 edit_tournament       🏆
        │
 🔄 5. Decks                 🟢 create_deck, add_version, set_status         🃏
        │                    ⚪ edit_deck, add_deck_alias
        │
 ⬜ 6. Lancement minimal     un module par service dans __main__.py          ▶️
        │
 ⬜ 7. Essai + doc           test_tournoi, copie de RelicFest ; PR           🚀
```

Chaque service est **indépendant** : on peut changer l'ordre des étapes 2 à 5 sans rien casser.

## 🧱 Les fichiers, couche par couche

Même architecture que le reste : chaque couche n'utilise que celles du dessous.

```
src/dcprepa/
├── __main__.py                ⬜ un module par service de saisie ; en-tête à corriger ▶️
├── services/
│   ├── games.py               ✅ add_games(), edit_game(), edit_match(),      🎮
│   │                             delete_game(), delete_match()
│   ├── oppos.py               ✅ add_oppo()                                   ⚔️
│   ├── tournament.py          ✅ create_tournament(), edit_tournament()       🏆
│   └── decks.py               ✅ create_deck(), add_version(), set_status(),  🃏
│                                 edit_deck(), add_deck_alias()
├── domain/                    (règles pures, sans fichier)
│   ├── validation.py          🔁 validate_block() réutilisé tel quel          ✅
│   ├── games.py / rows.py     🔁 parse_bos(), build_rows() réutilisés         ✅
│   └── saisie.py              ✅ slugify, contrôles tournoi, deck, liste, version 🧮
└── storage/                   (seul accès à data/)
    ├── games.py               🔁 append_rows() réutilisé ; ⬜ réécrire games.csv (.tmp) 📂
    ├── oppos.py               🔁 append_oppos() réutilisé                     ✅
    ├── tournament.py          ✅ copier le modèle, écrire tournament.yaml     📂
    ├── yaml_text.py           ✅ set_fields, yaml_scalar (champ remplacé, commentaire gardé) 📂
    └── decks.py               ✅ écrire une fiche, une version, un champ, un alias 📂

src/tests/                     ⬜ un fichier de tests par fichier de code    🧪
```

Réutilisation : l'import de l'inbox vérifie déjà un bloc (date, source, deck, version, oppo, games) et l'écrit dans `games.csv`.
`add_games()` reçoit **le même bloc** (un formulaire de la GUI = un bloc d'inbox) et passe par **les mêmes contrôles**.

## 🔎 Les étapes en détail

### ✅ 1. Décisions 🤔

Tranché par l'utilisateur (2026-09-24) :

- ✅ **Services retenus** : tous ceux des tableaux ci-dessus (🟢, 🟡 et ⚪).
- ✅ **Ordre** : par priorité, games d'abord (usage le plus fréquent).
- ✅ **Où écrire les games** : directement dans `games.csv`, avec les contrôles de l'import.
  L'inbox reste la saisie sur téléphone (GitHub), importée comme aujourd'hui.

```
 téléphone ──> inbox.yaml ──> import ──┐
                                       ├──> mêmes contrôles ──> games.csv
 GUI ──> formulaire ──> add_games() ───┘
```

- ✅ **Écriture des fiches deck** : modifier le texte du fichier (ajout en fin, remplacement d'une ligne `statut:`),
  comme `append_oppos`, pour garder les commentaires ; pas de réécriture complète par `yaml.dump`.
- ✅ **Slug d'un tournoi** : déduit du nom, pas demandé (« RelicFest 2026 » → `relicfest-2026`).
- ✅ **Corriger les games** : modifier une game ou un BO (ex. une game L → W), supprimer une game ou un BO.
- ✅ **Lancement minimal** : les services de saisie sont exposés dans `__main__.py`.

Détails validés par l'utilisateur (2026-09-24) :

- ✅ **Slug** : minuscules, accents retirés, tout caractère autre que lettre ou chiffre → `-` ; dossier déjà existant = erreur
  (pas de suffixe automatique `-2`, pour ne pas créer un doublon sans le voir).
- ✅ **Champs d'un tournoi** : `name` obligatoire ; `date`, `location` facultatifs ; `format` = `Duel Commander` par défaut ;
  `banlist` et `notes` via `edit_tournament`.
- ✅ **Modifier une game** : `edit_game` change position, résultat, note ; `edit_match` change date, source, deck, version, oppo
  pour toutes les games du BO, avec les contrôles de l'import. Date changée → nouveau `match_id` (prochain numéro libre de la nouvelle date).
- ✅ **Supprimer une game** : les games suivantes du BO sont renumérotées (3 → 2) ; un BO3 réduit à une game devient un BO1 → avertissement.
- ✅ **Exposer les services** : arguments en ligne de commande avec `argparse` (bibliothèque standard, aucune dépendance),
  un module par service, sans questions interactives, ex. `python -m dcprepa tournoi-creer "RelicFest 2026" --date 31/10/2026`.
  Remplace la lecture actuelle de `sys.argv` ; les modules existants (`import`, `meta`, `stats`) gardent la même commande.
- ✅ **Qui code** : Claude code et explique chaque étape (quoi, à quoi ça sert) ; l'utilisateur supervise et valide avant la suivante.

### ✅ 2. Games 🎮

- ✅ Refactor validé : `domain/blocks.py::prepare_block()` (un bloc → lignes, erreurs, avertissements) et
  `services/games.py::load_game_references()` (decks, alias, oppos, match_id) ; `import_inbox()` les utilise, comportement inchangé.
- ✅ 🟢 `add_games(tournament_dir, block, oppos_path)` → `GamesReport` (BO, games, `match_ids` écrits, erreurs, avertissements) :
  mêmes contrôles et messages que l'import ; bloc invalide = rien écrit ; oppo inconnu = avertissement.
- ✅ Tests : `tests/services/test_games.py` (11) et `tests/domain/test_blocks.py` (4) ; suite : 526 OK.
- ✅ Commité (`70358e3`, avec la doc de début de branche).
- ✅ `domain/edits.py` : `edit_game`, `edit_match`, `delete_game`, `delete_match` sur la liste des games (pur) → `EditResult`.
  Principe : le BO corrigé est reconstruit en bloc et repasse par `prepare_block` (mêmes contrôles que l'import) ;
  notes par game gardées ; `match_id` gardé sauf date changée ; BO remis à sa place dans le fichier.
  Une correction qui rend le BO impossible est refusée (ex. W L W → W W W : « game 3 : en trop, BO déjà terminé (2-0) »).
- ✅ Services `services/games.py` : `edit_game(t, match_id, game, changes, oppos)`, `edit_match(t, match_id, changes, oppos)`,
  `delete_game(t, match_id, game)`, `delete_match(t, match_id)` → `GamesReport` ; `storage/games.py::write_games` (réécriture via `.tmp`, fins de ligne gardées).
- ✅ Essai sur une copie de `test_tournoi` : seule la ligne visée change, `stats` relit sans erreur.
- ✅ Tests : bloc valide (BO3 + BO1), deck ou version inconnus, oppo inconnu, self-play, `match_id` qui suivent ceux du jour ;
  game corrigée (L → W), valeur invalide refusée, BO corrigé (toutes ses games), date changée (nouveau `match_id`),
  game supprimée (renumérotation), BO supprimé, `match_id` ou game inconnus, autres lignes intactes.
- ✅ Tests : `tests/domain/test_edits.py` (19), `tests/services/test_games.py` (17 au total) ; suite : 551 OK.
- ✅ Commité (`6552503`).

### ✅ 3. Oppos ⚔️

- ✅ 🟡 `services/oppos.py::add_oppo(oppos_path, name, variant_of=None)` → `OppoReport` (name, reference, errors) :
  nouvel oppo à la fin du fichier (`append_oppos`, commentaire « Ajouté par la saisie le JJ/MM/AAAA ») ;
  variante insérée sous sa référence (`storage/oppos.py::insert_variant`, texte relu avant écriture, commentaires gardés).
- ✅ Contrôles (`domain/oppos.py::check_new_oppo`) : nom vide, « @ » (self-play), nom déjà connu (référence ou variante),
  référence inconnue ; `variant_of` accepte toute forme connue (« raga » → Ragavan).
- ✅ Tests : domaine (8), stockage (5), service (8, dont le vrai `data/oppos.yaml` copié) ; suite : 572 OK.
- ✅ Commité (`330d4dc`).

### ✅ 4. Tournoi 🏆

- ✅ 🟢 `services/tournament.py::create_tournament(data_dir, name, fields)` → `TournamentReport` (slug, folder, errors) :
  slug déduit du nom (`domain/saisie.py::slugify`) ; copie du modèle dans `<slug>.tmp` puis renommage (tout ou rien) ;
  retire `README.md`, `stats/_modele-deck.md` et sa ligne de `stats/README.md` (consignes du README du modèle) ; garde `decks/_modele.yaml`.
- ✅ 🟡 `edit_tournament(tournament_dir, changes)` : name, format, date, location, banlist, notes ; slug et dossier inchangés.
- ✅ Utilitaire `storage/yaml_text.py::set_fields` : remplace la valeur d'un champ en gardant son commentaire aligné, texte relu avant écriture
  (réutilisable pour `set_status`) ; `yaml_scalar` (guillemets seulement si YAML l'exige).
- ✅ Contrôles (`check_tournament_fields`) : nom vide ou sans lettre ni chiffre, champ inconnu (slug compris), date `JJ/MM/AAAA` ; dossier déjà présent.
- ✅ Tests : `domain/test_saisie.py` (15), `storage/test_yaml_text.py` (12), `storage/test_tournament.py` (+2), `services/test_tournament.py` (10,
  dont tournoi créé qui passe `stats`, RelicFest copié modifié sans perte) ; suite : 611 OK.
- ✅ Commité (`b663733`).

### 🔄 5. Decks 🃏

Choix de Claude (dans la suite des décisions de l'étape 1, à confirmer à la relecture) : fichier déduit du nom comme le slug d'un tournoi ;
version suivante automatique (v3 → v4) ou imposée ; liste de v1 facultative (avertissement si pas 100 cartes) ; deck désigné par n'importe quelle appellation.

- ✅ 🟢 `services/decks.py::create_deck(t, name, commandant, statut, liste, notes)` → `DeckReport` (deck, version, errors, warnings) :
  `decks/<slug>.yaml` écrit avec v1 (en-tête « Fiche deck créée par la saisie le … »).
- ✅ 🟢 `add_version(t, deck, version=None, cards_in, cards_out, liste, notes)` : version ajoutée après la dernière ligne réelle du bloc
  `versions` (un exemple commenté reste en dessous), texte relu (versions = anciennes + la nouvelle, autres champs inchangés).
- ✅ 🟢 `set_status(t, deck, statut)` ; ⚪ `edit_deck(t, deck, {name, commandant})` (fichier inchangé) ; ⚪ `add_deck_alias(t, deck, alias)`.
- ✅ Contrôles (`domain/saisie.py`) : `check_new_deck` (vide, réservé synthese / README, appellation déjà prise), `check_status`,
  `parse_card_list` (« 1 Nom de carte »), `next_version`, `check_new_version` (déjà présente, espace, aucun changement).
- ✅ Stockage (`storage/decks.py`) : `version_lines`, `create_deck_file`, `append_version`, `update_deck_fields`, `add_alias` ;
  `storage/yaml_text.py::append_list_item` (générique, réutilisé par `oppos.insert_variant`, dont le doublon `_yaml_scalar` est retiré).
- ✅ Essai sur une copie de RelicFest : diff de la vraie fiche Terra = statut + v2 seulement.
- ✅ Tests : domaine (+19), stockage (+7), service (15, dont version ajoutée puis saisie et stats) ; suite : 652 OK.
- ⬜ Relecture et commit par l'utilisateur.

### ⬜ 6. Lancement minimal ▶️

- ⬜ Un module par service de saisie dans `__main__.py` (`argparse`, arguments sans questions interactives), bilan `✅ / ❌ / ⚠️` comme les autres modules.
- ⬜ En-tête de `__main__.py` : retirer « en attendant la vraie CLI ».

### ⬜ 7. Essai et livraison 🚀

- ⬜ Essai sur une copie de `test_tournoi` et de RelicFest : saisir, puis `stats` sans erreur.
- ⬜ Suite pytest complète verte.
- ⬜ `src/README.md` : services ajoutés ; README de `data/` concernés à jour.
- ⬜ Doc (brouillon `docs/.claude_brouillon_<page>.md`, publication sur validation) : page de `docs/03-architecture/`.
- ⬜ PR `feat/saisie` → `main`.

## 🔭 Après cette branche

Voir `docs/.claude_feuille_de_route.md` pour l'ensemble des branches.

```
✅ stats-synthese (#6) ─> 🔄 feat/saisie ─> ⏸️ feat/gui
                           (cette branche)   interface graphique + services de lecture
```
