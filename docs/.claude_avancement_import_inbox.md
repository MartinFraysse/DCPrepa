# Avancement — import de l'inbox

> Schémas de l'import de l'inbox : ce qui est codé, ce qui ne l'est pas, et le comportement de chaque étape.
> Fichier privé de Claude (comme `.claude_doc.md`) : jamais dans le PDF ni dans `~/work`.

**État au :** 23/09/2026 · 141 tests pytest OK · `storage/inbox.py` terminé (`read_inbox` + `clear_inbox`)

Légende : ✅ codé et testé · 🟡 codé, pas encore commité · ⬜ à faire

## 1. Vue d'ensemble du projet

```
 FORMAT DES DONNÉES        IMPORT DE L'INBOX         STATS              MÉTA            GUI
 ✅ terminé                 🟡 environ 60 %            ⬜                 ⬜              ⬜
 tournament.yaml            inbox.yaml → games.csv    games.csv →        MTGTop8 →       interface
 decks/, inbox, games.csv                             stats/*.md         meta/*.csv      graphique
 oppos.yaml, template
        │                         │                        │                 │
        └─────────────── on est ici ▲ ────────────────────────────────────────┘
```

## 2. Les fichiers de code, rangés par couche

```
src/dcprepa/
├── interfaces/                          ⬜ rien encore
│   └── cli/            commande « import »                           ⬜
├── services/                            ⬜ rien encore
│   └── import_inbox.py enchaîne toutes les étapes (tout ou rien)     ⬜
├── domain/             règles pures, aucun fichier
│   ├── games.py        parse_bos / _parse_games                      ✅ 30 tests
│   ├── validation.py   validate_block                                ✅ 50 tests
│   ├── oppos.py        normaliser l'oppo                             ⬜
│   └── rows.py         bloc → lignes games.csv + match_id            ⬜
└── storage/            seul accès aux fichiers
    ├── decks.py        load_decks                                    ✅ 27 tests
    ├── inbox.py        read_inbox                                    ✅ 25 tests
    │                   clear_inbox (vide, garde l'en-tête)           ✅  9 tests
    ├── oppos.py        lire data/oppos.yaml                          ⬜
    └── games.py        lire games.csv, ajouter des lignes            ⬜
```

## 3. Le flux d'import, étape par étape

```
             ┌────────────────────────────────────────────────────────────┐
  ÉTAPE 1    │ LIRE                                                        │
             │  read_inbox(inbox.yaml)         ✅  → [bloc, bloc, …]      │
             │  load_decks(tournoi)            ✅  → {deck: [versions]}   │
             │  lire data/oppos.yaml           ⬜  → {référence: [variantes]}│
             │  lire games.csv                 ⬜  → match_id déjà utilisés│
             └──────────────────────────┬─────────────────────────────────┘
                                        ▼
             ┌────────────────────────────────────────────────────────────┐
  ÉTAPE 2    │ VÉRIFIER CHAQUE BLOC                                        │
             │  validate_block(bloc, decks)    ✅                          │
             │     ├─ champs obligatoires, date, source, deck, version    │
             │     └─ parties → parse_bos → _parse_games   ✅              │
             │  normaliser l'oppo (« raga » → « Ragavan »)  ⬜             │
             └──────────────────────────┬─────────────────────────────────┘
                                        ▼
             ┌────────────────────────────────────────────────────────────┐
  ÉTAPE 3    │ DÉCISION : TOUT OU RIEN               ⬜ (service)          │
             │   au moins 1 erreur, n'importe où ?                         │
             │      OUI → afficher TOUTES les erreurs, STOP               │
             │            games.csv et inbox.yaml ne sont PAS touchés     │
             │      NON → on continue                                      │
             └──────────────────────────┬─────────────────────────────────┘
                                        ▼
             ┌────────────────────────────────────────────────────────────┐
  ÉTAPE 4    │ TRANSFORMER                           ⬜                    │
             │  chaque BO → un match_id (AAAA-MM-JJ-NN, suite de games.csv)│
             │  chaque game → une ligne de games.csv                       │
             └──────────────────────────┬─────────────────────────────────┘
                                        ▼
             ┌────────────────────────────────────────────────────────────┐
  ÉTAPE 5    │ ÉCRIRE (dans cet ordre)                                     │
             │  1. ajouter les lignes à games.csv                  ⬜      │
             │  2. clear_inbox : vide inbox.yaml, garde l'en-tête  ✅      │
             │     (écrit un .tmp puis le renomme : jamais à moitié)       │
             └──────────────────────────┬─────────────────────────────────┘
                                        ▼
  ÉTAPE 6      CLI : « 3 blocs importés, 7 games » ou la liste des erreurs  ⬜
```

## 4. Le trajet d'un bloc, avec ce qui existe déjà

```
inbox.yaml                         read_inbox ✅                validate_block ✅
┌──────────────────────────┐      ┌──────────────────────┐     ┌─────────────────┐
│date: 02/10/2026          │      │{"date": "02/10/2026",│     │ []  → valide    │
│source: paper             │ ───▶ │ "source": "paper",   │ ──▶ │                 │
│deck: terra-midrange      │      │ "deck": "terra-…",   │     │ sinon :         │
│version: v1               │      │ "version": "v1",     │     │ ["deck inconnu…",│
│oppo: raga                │      │ "oppo": "raga",      │     │  "parties : BO 2 │
│parties: OTP W, OTD W /   │      │ "parties": "…"}      │     │  : game 1 : …"] │
│         OTD L            │      └──────────────────────┘     └─────────────────┘
└──────────────────────────┘                │
                                            ▼  parse_bos ✅
                              [[["OTP","W"],["OTD","W"]],  [["OTD","L"]]]
                                   BO 1 (2-0)                  BO 2 (BO1)
                                            │
                                            ▼  ⬜ normaliser oppo + transformer
games.csv
date,match_id,partie,source,deck,version,oppo,position,resultat,note/ressenti
02/10/2026,2026-10-02-01,1,paper,terra-midrange,v1,Ragavan,OTP,W,
02/10/2026,2026-10-02-01,2,paper,terra-midrange,v1,Ragavan,OTD,W,
02/10/2026,2026-10-02-02,1,paper,terra-midrange,v1,Ragavan,OTD,L,
```

## 5. Reste à faire pour un import de bout en bout

| # | Brique | Couche | Dépend de |
|---|---|---|---|
| 1 | lire `data/oppos.yaml` et normaliser l'oppo | storage + domain | décision : oppo inconnu = avertissement ou erreur ? |
| 2 | lire `games.csv` (match_id existants) et ajouter des lignes | storage | — |
| 3 | bloc validé → lignes CSV avec `match_id` | domain | 1, 2 |
| 4 | service `import_inbox` : tout ou rien, puis écritures dans l'ordre | services | 1 à 3 |
| 5 | commande `import` | interfaces/cli | 4 |

Fait : vider l'inbox en gardant l'en-tête (`clear_inbox`).
